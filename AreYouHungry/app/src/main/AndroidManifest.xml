<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.areyouhungry">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".takeoutPage.ad1Activity"></activity>
        <activity android:name=".takeoutPage.FruitShopActivity" />
        <activity android:name=".takeoutPage.FreeSendActivity" />
        <activity android:name=".takeoutPage.ShoperActivity" />
        <activity android:name=".takeoutPage.SearchResultActivity" />
        <activity android:name=".takeoutPage.SearchActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>